'use strict';

describe('myApp.electronview module', function() {

  beforeEach(module('myApp.electronview'));

  describe('electronview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ElectronviewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});